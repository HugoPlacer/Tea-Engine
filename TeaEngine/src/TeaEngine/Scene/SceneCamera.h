#pragma once

#include "TeaEngine/Renderer/Camera.h"
namespace Tea {

    class SceneCamera : public Camera
    {
    public:
        SceneCamera();
        ~SceneCamera() = default;
    private:
    };

}